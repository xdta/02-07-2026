For my next step, I have a series of images doily_[n], where n ranges from 1 to 6. I actually want a landing page that shows doily_1 (png) centered on the page width on top of cloth-bg.jpg (which will remain the background through this whole thing). When a user clicks on the doily_1 image, the page will go through doily_1 to doily_5 images over 2.5 seconds and land on doily 6, which should be big enough for all the text to fit.